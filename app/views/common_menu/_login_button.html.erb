<div class="block md:ml-6">
  <div class="flex space-x-4">
    <% if @logged_in %>
      <%= button_to "ログアウト", session_path, method: :delete, class: "rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white" %>
    <% end %>
  </div>
</div>
