<div class="w-9/10">
  <div class="w-full my-[1em] sticky">
    <div class="flex justify-between items-center sticky">
      <div class="text-2xl font-bold">
        リスト一覧
      </div>
      <%# 作成ボタン %>
      <%= link_to "+ 作成", new_list_path, class: "grid place-items-center w-[5em] h-[3em] rounded-lg bg-blue-600 hover:bg-blue-500 text-white" %>
    </div>
  </div>

  <div class="w-full mt-[1em] overflow-x-auto">
    <% @lists.each do |list| %>
      <%= render list %>
    <% end %>
  </div>
</div>
